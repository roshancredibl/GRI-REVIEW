import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <div className="header-title">GRI Framework Dashboard</div>
      <div className="account-avatar">AM</div>
    </header>
  );
};

export default Header;
